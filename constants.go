package libext

const (
	// RootInode is the conventional root directory inode in EXT.
	RootInode uint32 = 2

	// SuperblockOffset is the byte offset of the primary superblock.
	SuperblockOffset uint64 = 1024
)
